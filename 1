extern crate tts;

use tts::SoundService;

use std::thread;
use std::mpsc;

pub fn main(){
    let sound =  SoundService::new();
    let (tx, rx) = mpsc::channel();
    thread::spawn( move || { sound.run(
}
